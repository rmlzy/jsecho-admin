import { UserGroupPipe } from './user-group.pipe';

describe('UserGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new UserGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
